[package]
name = "ai-delver-level"
version = "0.1.0"
edition = "2021"

[lib]
name = "ai_delver_level"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
